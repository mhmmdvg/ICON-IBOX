import 'package:flutter/material.dart';

class PopScreen extends StatelessWidget {
  static String routeName = "/pop_screen";
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Text('Push Sukses'),
    );
  }
}